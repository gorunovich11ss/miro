UnauthorizedError:
  description: You are unauthorized
  content:
    application/json:
      schema:
        $ref: "#/schemas/Error"

NotFoundError:
  description: Resorce not found
  content:
    application/json:
      schema:
        $ref: "#/schemas/Error"

BadRequestError:
  description: Bad request
  content:
    application/json:
      schema:
        $ref: "#/schemas/Error"

schemas:
  Error:
    type: object
    required:
      - message
      - code
    properties:
      message:
        type: string
      code:
        type: string